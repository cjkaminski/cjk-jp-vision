# --- Core server (always needed) ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9   # form/file uploads
Pillow>=10.0              # image decode / preprocessing
httpx>=0.27               # talking to local LLM / Anthropic API
pydantic>=2.6

# --- OCR backends (optional; install what your platform supports) ---
# manga-ocr: works on any platform (Mac/Linux/Win). Pulls in torch.
#   pip install manga-ocr
#
# Apple Vision (macOS only) needs pyobjc Vision/Quartz bindings:
#   pip install pyobjc-framework-Vision pyobjc-framework-Quartz

# --- Tokenizer (optional but recommended for word breakdown) ---
# fugashi + unidic-lite gives a self-contained MeCab tokenizer:
#   pip install fugashi unidic-lite

# --- Analysis backends ---
# claude_agent (preferred, plan auth via Agent SDK — no API key):
#   pip install claude-agent-sdk
#   npm install -g @anthropic-ai/claude-code   # the engine it drives
#   then run `claude` once to sign in. Leave ANTHROPIC_API_KEY UNSET for plan billing.
# claude_cli (fallback, also plan auth): just the Claude Code CLI above (no pip pkg).
# anthropic_api (optional, API-key billing): set ANTHROPIC_API_KEY (httpx, no SDK needed)
# local_llm: no extra deps here — talks to an OpenAI-compatible endpoint via httpx
